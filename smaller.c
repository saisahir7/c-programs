#include<stdio.h>
int main()
{
	int a,b;
	printf("enter the values");
	scanf("%d%d",&a,&b);
	if(a>b)
	{ 
	printf("a is grater than b");
	}
    else
    { 
    printf("a is smaller than b");
	}
	return(0);
}
